<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Blog Website</title>
    <style>
        .btn-modern {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            border: none;
            border-radius: 50px;
            color: #fff;
            padding: 0.5rem 1.5rem;
            font-weight: 500;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .btn-modern:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
        }
        .btn-create {
            background: linear-gradient(135deg, #ff7e5f 0%, #feb47b 100%);
        }
        .btn-read {
            background: linear-gradient(135deg, #4caf50 0%, #8bc34a 100%);
        }
        .btn-edit {
            background: linear-gradient(135deg, #00bcd4 0%, #03a9f4 100%);
        }
        .btn-delete {
            background: linear-gradient(135deg, #f44336 0%, #e57373 100%);
        }
        .navbar-custom {
            background-color: #343a40;
        }
        .navbar-brand {
            font-size: 1.5rem;
            color: #ffffff !important;
            font-weight: 600;
        }
    </style>
</head>
<body>
    
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom">
        <div class="container">
            <div class="navbar-brand mx-auto">BLOGS</div>
        </div>
    </nav>

   
    <div class="container mt-4">
        <h1 class="mb-4">Blog Website</h1>
        <a href="/articles/new" class="btn btn-modern btn-create mb-4">Create Article</a>
        
        <% articles.forEach(article => {%>
           <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title">
                    <%=article.title%>
                </h4>
                <div class="card-subtitle text-muted mb-2">
                    <%=article.createdAt.toLocaleDateString()%>
                </div>
                <div class="card-text mb-2">
                    <%=article.description%>
                </div>
                <a href="articles/<%=article.slug%>" class="btn btn-modern btn-read">Read more..</a>
                <a href="articles/edit/<%=article._id%>" class="btn btn-modern btn-edit">Edit</a>
                <form action="/articles/<%=article._id%>?_method=DELETE" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-modern btn-delete">DELETE</button>
                </form>
            </div>
           </div>
        <%})%>
    </div>
</body>
</html>
