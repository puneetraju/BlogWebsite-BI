<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Blog Website</title>
    <style>
        .btn-modern {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            border: none;
            border-radius: 50px;
            color: #fff;
            padding: 0.5rem 1.5rem;
            font-weight: 500;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .btn-modern:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
        }
        .btn-save {
            background: linear-gradient(135deg, #00bcd4 0%, #03a9f4 100%);
        }
        .btn-cancel {
            background: linear-gradient(135deg, #ff7e5f 0%, #feb47b 100%);
        }
        .navbar-custom {
            background-color: #343a40;
        }
        .navbar-brand {
            font-size: 1.5rem;
            color: #ffffff !important;
            font-weight: 600;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom">
        <div class="container">
            <div class="navbar-brand mx-auto">BLOGS</div>
        </div>
    </nav>

    
    <div class="container mt-4">
        <h1 class="mb-4">Create New Article</h1>
        <form action="/articles" method="POST">
            <%- include('forms') %>
            <a href="/" class="btn btn-modern btn-cancel mt-3">Cancel</a>
            <button type="submit" class="btn btn-modern btn-save mt-3">Save</button>
        </form>
    </div>
</body>
</html>
